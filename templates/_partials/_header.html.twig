<header style="background-image:linear-gradient(to bottom, rgba(0, 0, 0, 1) 20%, rgba(0, 0, 0, 0.2)),url({{ asset('images/' ~ siteInfo.getSiteInfo.pictureName) }});background-size: cover; background-position: top; min-height:10em" class="relative">
    <nav class="px-4 py-2 flex justify-between">
        <a href="{{ path('app_main') }}" title="retour à la page d'accueil"><img src="{{ asset('images/logo/' ~ siteInfo.getSiteInfo.logoName) }}" alt="Logo de {{ siteInfo.getSiteInfo.title }}" class="h-4 w-4"></a>
        
    </nav>
</header>